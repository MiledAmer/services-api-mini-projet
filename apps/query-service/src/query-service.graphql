type Product @key(fields: "id") {
  id: ID!
  name: String!
  price: Float!
  stock: Int!
}

type Order @key(fields: "id") {
  id: ID!
  productId: ID!
  quantity: Int!
  email: String!
}

type Query {
  products: [Product!]!
  orders: [Order!]!
  orderById(id: ID!): Order
}